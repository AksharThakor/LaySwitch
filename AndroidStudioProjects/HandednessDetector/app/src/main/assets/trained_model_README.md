# Handedness Model



This project requires a trained TensorFlow Lite model (`.tflite`) generated from training the model with given script after collecting real IMU data using the __imuData_Handedness__ Android application.

Once you follow the main README file and collect data and run script,  you need to paste (`.tflite`) file here.

